# Substrate coordination mirror

Committed mirrors of the qwertty↔rabbitui drop-box (`../../../qwertty/` from
this file, i.e. `work/qwertty/` at the repo root). The drop-box is the live
exchange point and stays untracked; these copies are committed after each
exchange so no jj working-copy incident can lose the coordination history
again. Update flow: edit/receive in the drop-box, then `cp` here and commit.
